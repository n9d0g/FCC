import React, { FC } from 'react'
import { Button } from '@material-ui/core'

const GiveFormSubmit: FC = () => {
    return (
        <div>
            <Button type="submit">Submit</Button>
        </div>
    )
}

export default GiveFormSubmit
